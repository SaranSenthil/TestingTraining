package org.stepdefinition;
import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;
import org.sa.BaseClass;
import org.sam.Fbloginpojo;




public class positivenegativeclass extends BaseClass {

	/*
	 * Fbloginpojo f;
	 * 
	 * @Given("User has to launch browser and maximize the window") public void
	 * user_has_to_launch_browser_and_maximize_the_window() { // Write code here
	 * that turns the phrase above into concrete actions launchBrowser();
	 * windowMaximize(); }
	 * 
	 * @When("User has to hit the FB URL") public void user_has_to_hit_the_FB_URL()
	 * { launchUrl("https://www.gmail.com/"); }
	 * 
	 * @When("To pass<{string}> User name in email field") public void
	 * to_pass_User_name_in_email_field(String em) { f=new Fbloginpojo(); //
	 * passText(em, f.getEmail()); }
	 * 
	 * @When("To pass <{string}> PWD on passwordfield") public void
	 * to_pass_PWD_on_passwordfield(String pass) { f=new Fbloginpojo();
	 * //passText(pass, f.getPassword()); }
	 */

}
